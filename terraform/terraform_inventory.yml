plugin: community.general.terraform
# Path to your remote state file in S3
remote_state:
  backend: s3
  config:
    bucket: "zeelz-terraform-bucket"
    key: "terraform.tfstate"
    encrypt: true
    region: "us-east-1"