stages:
  - ansible

image:
  name: registry.gitlab.com/gitlab-org/terraform-images/stable:latest
  entrypoint: [""]

ansible:
  stage: ansible
  script:
    # Create inventory file
    - echo "[ec2]" > inventory.ini
    - echo "$SSH_PRIVATE_KEY" >> inventory.ini
    - cat inventory.ini
